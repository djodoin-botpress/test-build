version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Yarn Build"
          command: "yarn build"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
